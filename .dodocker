# Commands to be executed during testing
# Lines starting with 'docker:' will be executed in a Docker container
# All other commands will be executed in the local shell

# Install dependencies (runs in local shell)
pip install -e .

# Run tests (runs in local shell)
pytest -v

# Run linters (runs in local shell)
black --check .
isort --check-only .
flake8
mypy .

# Test the CLI (runs in local shell)
python -m domd --help
python -m domd --version

# Example of a command that should run in Docker
# docker: some-command-that-needs-docker
